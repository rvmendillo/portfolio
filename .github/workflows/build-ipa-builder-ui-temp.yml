name: Temporary Build IPA Builder UI

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-ipa-builder-ui-temp.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout IPA Builder branch
        uses: actions/checkout@v6
        with:
          ref: ipa-builder-build-20260903

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate project
        working-directory: ipa-builder-build
        run: xcodegen generate

      - name: Resolve packages
        working-directory: ipa-builder-build
        run: |
          xcodebuild -resolvePackageDependencies \
            -project IPABuilder.xcodeproj \
            -scheme IPABuilder

      - name: Build unsigned IPA Builder
        working-directory: ipa-builder-build
        run: |
          set -o pipefail
          xcodebuild \
            -project IPABuilder.xcodeproj \
            -scheme IPABuilder \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            clean build | tee xcodebuild.log

      - name: Package IPA
        working-directory: ipa-builder-build
        run: |
          rm -rf Payload
          mkdir Payload
          cp -R build/Build/Products/Release-iphoneos/IPABuilder.app Payload/
          ditto -c -k --sequesterRsrc --keepParent Payload IPABuilder-1.1-unsigned.ipa
          unzip -t IPABuilder-1.1-unsigned.ipa
          test -f Payload/IPABuilder.app/IPABuilder

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: IPABuilder-1.1-unsigned-ipa
          path: ipa-builder-build/IPABuilder-1.1-unsigned.ipa
          if-no-files-found: error
          retention-days: 7
